# STM32 Timer-Based LED Blink

**Description**

This project demonstrates the implementation of the general-purpose timers (TIM) using the counter, prescaler, and auto-reload registers to develop a timer driver for TIM2 to generate a 1Hz timeout.
